architecture: standalone

auth:
  enabled: true

master:
  persistence:
    enabled: false

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 100m
      memory: 256Mi

replica:
  replicaCount: 0

metrics:
  enabled: false

service:
  type: ClusterIP